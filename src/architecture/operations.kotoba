(ns architecture.operations
  "The closed vocabulary of design operations this actor may propose.

  Before this catalog the governor judged a proposal by its `:effect`, its
  project and a deny-list of two ops — and accepted every other `:op` it had
  never heard of. The advisor is swappable and `llm-advisor` passes through
  whatever `:op` the model wrote, so `{:op :demolish-site :effect :propose
  :confidence 0.9}` for a registered project came back `:ok? true` and was
  committed as a design record. A deny-list only refuses what somebody
  thought of; this is the allow-list the governor now checks against.

  Three kinds of op:
    permitted  — may be proposed; the governor still applies its other rules.
    forbidden  — named, and always a hard `:no-stamping` block: the licensed
                 landscape architect's exclusive responsibility.
    anything else — unknown, and a hard `:unknown-op` block. That includes
                 `:unknown`, which `advisor/parse-proposal` emits when the
                 model's reply could not be read: there is no proposal to
                 approve, so it holds rather than asking a human to sign off
                 on nothing.")

(def permitted
  "Ops the advisor may propose, with what each one produces."
  {:draft-design-concept               "a draft design concept for a registered project"
   :prepare-specification              "a draft material / planting specification"
   :flag-environmental-compliance-issue "an environmental concern for the architect to review (always escalates)"
   :request-client-review              "client review material"})

(def forbidden
  "Ops that name the architect's professional seal. Never permitted, never
  escalatable."
  #{:issue-stamped-design :certify-environmental-compliance})

(defn permitted? [op] (contains? permitted op))

(defn forbidden? [op] (contains? forbidden op))

(defn unknown?
  "True for an op that is neither permitted nor forbidden."
  [op]
  (not (or (permitted? op) (forbidden? op))))
